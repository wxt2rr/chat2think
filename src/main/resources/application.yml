spring:
  datasource:
    url: jdbc:mysql://master.mysql.account-center.local.chuangkit.com:3306/account_center?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: local_account_center_rw
    password: zF3eT64DZKS9idFXpSq4Yy32Bj9ZaFhf
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  redis:
    host: master.local.redis.chuangkit.com
    port: 6379
    password: account-center-sandbox@VIR3HzLqO5FWz7LluGcQ59RkWQfsTM
  messages:
    basename: i18n/messages
    encoding: UTF-8
  main:
    allow-circular-references: true
  servlet:
    multipart:
      max-file-size: 100MB
      max-request-size: 100MB

server:
  port: 8080

app:
  jwt:
    secret: chat2think-secret-key
    expiration: 86400000 # 24 hours
  image:
    upload:
      dir: uploads/images/
    models:
      - name: stable-diffusion
        api-url: http://localhost:7860
      - name: dalle
        api-url: https://api.openai.com/v1/images/generations
        api-key: ${OPENAI_API_KEY}
        model: dall-e-3

